package megalodonte.base;

public class Utility {
//    public static boolean detectDevMode() {
//        // Em produção (jpackage), a app roda de dentro de um .exe/.app/.deb
//        // Em dev, roda via `gradle run` ou pela IDE — sem o diretório de instalação
//        String appDir = System.getProperty("app.dir"); // jpackage define isso automaticamente
//        if (appDir != null) return false;
//
//        // Fallback: verifica se está rodando de dentro de um jar empacotado
//        String classLocation = Utility.class.getProtectionDomain()
//                .getCodeSource()
//                .getLocation()
//                .toString();
//
//        // Em dev, o caminho aponta para build/classes/... ou é executado pela IDE
//        return classLocation.contains("build/classes") || classLocation.contains("bin/");
//    }
}
